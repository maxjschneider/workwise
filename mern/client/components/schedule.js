import React, { useEffect, useState } from "react"

import { Link } from 'react-router-dom'

function generateScheduleRow(props) {
    var obj;

    Object.keys(props.user.schedule).forEach(x => obj = props.user.schedule.)
}